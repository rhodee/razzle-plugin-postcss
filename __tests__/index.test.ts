// import modify from '../index'
// import { cssConfig } from '../testdata/cssConfig'

/*tslint:disable-next-line:no-var-requires no-submodule-imports*/
// const createConfig = require('razzle/config/createConfig')

describe('razzle-plugin-postcss', () => {
  describe('properly configured in development', () => {
    xit('appends CSS extract plugin and loaders', () => {
      // const pluginFunc = modify({ postcssPlugins: cssConfig.postcssPlugins, cssFilename: cssConfig.cssFilename, cssChunkFilename: cssConfig.cssChunkFilename })
      // const config = createConfig('web', 'dev', {
      //   plugins: [{ func: pluginFunc, options: { useBabel: false } }]
      // })
      // const nodeConfig = createConfig('node', 'dev', {
      //   plugins: [{ func: pluginFunc, options: { useBabel: false } }]
      // })
    })
  })

  describe('properly configured in production', () => {
    xit('appends CSS extract plugin and loaders', () => {
      // const pluginFunc = modify({ postcssPlugins: cssConfig.postcssPlugins, cssFilename: cssConfig.cssFilename, cssChunkFilename: cssConfig.cssChunkFilename })
      // const config = createConfig('web', 'prod', {
      //   plugins: [{ func: pluginFunc, options: { useBabel: false } }]
      // })
      // const nodeConfig = createConfig('node', 'dev', {
      //   plugins: [{ func: pluginFunc, options: { useBabel: false } }]
      // })
    })
  })
})
